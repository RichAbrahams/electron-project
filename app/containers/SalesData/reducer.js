import {
} from './constants';

const initialState = {
  testdata: 'reducer/SalesData',
};

function ConsignmentsReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default ConsignmentsReducer;
