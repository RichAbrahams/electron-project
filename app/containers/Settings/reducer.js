import {
} from './constants';

const initialState = {
  testdata: 'SettingsReducer',
};

function SettingsReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default SettingsReducer;
