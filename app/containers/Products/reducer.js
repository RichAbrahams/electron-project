import {
} from './constants';

const initialState = {
  testdata: 'ProductsReducer',
};

function ProductsReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default ProductsReducer;
